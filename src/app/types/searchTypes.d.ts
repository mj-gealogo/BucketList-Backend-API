type countrySearchQuery = {
    q?: string,
    sortBy?: string
    count?: number,
    startIndex?: number
}

type placeSearchQuery = {
    q?: string,
    sortBy?: string
    count?: number,
    startIndex?: number
}


type activitySearchQuery = {
    q?: string,
    sortBy?: string
    count?: number,
    startIndex?: number
}